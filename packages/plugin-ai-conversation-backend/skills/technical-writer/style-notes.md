House style:

- US spelling. Oxford comma. Sentence case for headings.
- Code, commands, paths, and identifiers in `backticks`.
- "you" for the reader; avoid "we" except for genuinely shared decisions.
- One idea per paragraph; a list when there are three or more parallel items.
